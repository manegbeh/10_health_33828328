<!DOCTYPE html>
<html>
<head>
  <title>Edit Workout – <%= shopData.shopName %></title>
  <link rel="stylesheet" href="/main.css">
</head>
<body>
  <nav class="navbar">
    <div class="nav-left">
      <a href="/" class="nav-logo"><%= shopData.shopName %></a>
    </div>

    <ul class="nav-center">
      <li><a href="../about">About</a></li>
      <li><a href="./add">Add Workouts</a></li>
      <li><a href="./list">All Workouts</a></li>
      <li><a href="./search">Search Workouts</a></li>
      <li><a href="./timer">Workout Timer</a></li>
    </ul> 
      
    <div class="nav-right">
      <a href="../auth/login" class="login-btn">Login</a>
      <a href="../auth/logout" class="login-btn">Logout</a>
    </div>

  </nav>
<div class="index-container">
  <div class="section">
    <h1>Edit Workout</h1>

    <form action="/workouts/edit/<%= workout.id %>" method="POST">
      <label>Workout Name</label>
      <input type="text" name="name" value="<%= workout.name %>" required>

      <label>Activity</label>
      <select name="activity" required>
        <option value="walking" <%= workout.activity === "walking" ? "selected" : "" %>>Walking</option>
        <option value="running" <%= workout.activity === "running" ? "selected" : "" %>>Running</option>
        <option value="cycling" <%= workout.activity === "cycling" ? "selected" : "" %>>Cycling</option>
        <option value="swimming" <%= workout.activity === "swimming" ? "selected" : "" %>>Swimming</option>
        <option value="weightlifting" <%= workout.activity === "weightlifting" ? "selected" : "" %>>Weightlifting</option>
        <option value="hiit" <%= workout.activity === "hiit" ? "selected" : "" %>>HIIT</option>
        <option value="yoga" <%= workout.activity === "yoga" ? "selected" : "" %>>Yoga</option>
      </select>

      <label>Calories Burned</label>
      <input type="number" step="1" name="calories" required>

      <label>Time</label>
      <input type="number" step="0.01" name="time" required>

      <div class="actions">
        <button class="btn btn-primary" type="submit">Save</button>
        <a class="btn" href="../workouts/list">Cancel</a>
      </div>
    </form>
  </div>

</div>
</body>
</html>
